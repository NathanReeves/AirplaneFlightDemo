public enum PlaneState
{
    Flying,
    Crashed
}

public enum JetState
{
    Off,
    Idle,
    Thrust,
    Boost
}

public enum SoundType
{
    Engine,
    Missile,
    Asteroid,
    PlaneCrash,
    ChallengeComplete,
    GameComplete
}